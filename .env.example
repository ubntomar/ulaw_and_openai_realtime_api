# ====================================================================
# CONFIGURACIÓN DE VARIABLES DE ENTORNO
# Sistema de Llamadas Entrantes con OpenAI Realtime API
# ====================================================================

# --------------------------------------------------------------------
# ASTERISK ARI CONFIGURATION
# --------------------------------------------------------------------
# Usuario configurado en /etc/asterisk/ari.conf
ASTERISK_USERNAME=tu_usuario_ari

# Contraseña del usuario ARI
ASTERISK_PASSWORD=tu_password_ari

# Host donde está corriendo Asterisk (localhost o IP del servidor)
ASTERISK_HOST=localhost

# Puerto de la API ARI (por defecto 8088)
ASTERISK_PORT=8088

# --------------------------------------------------------------------
# OPENAI REALTIME API CONFIGURATION
# --------------------------------------------------------------------
# API Key de OpenAI (obtener en https://platform.openai.com/api-keys)
OPENAI_API_KEY=sk-proj-your-key-here

# Modelo de OpenAI Realtime (OPCIONAL - por defecto usa gpt-4o-realtime-preview-2024-12-17)
# Opciones disponibles:
# - gpt-4o-realtime-preview-2024-12-17 (recomendado, estable)
# - gpt-4o-realtime-preview-2024-10-01 (versión anterior)
# Descomenta la siguiente línea solo si quieres usar un modelo diferente:
# OPENAI_REALTIME_MODEL=gpt-4o-realtime-preview-2024-12-17

# --------------------------------------------------------------------
# NETWORK CONFIGURATION
# --------------------------------------------------------------------
# Dirección IP local del servidor para RTP (usar la IP del servidor)
# Ejemplo: 192.168.1.100 o usar $(hostname -I | awk '{print $1}')
LOCAL_IP_ADDRESS=192.168.1.100

# --------------------------------------------------------------------
# LOGGING CONFIGURATION
# --------------------------------------------------------------------
# Ruta completa del archivo de log
LOG_FILE_PATH=/var/log/asterisk/inbound_openai.log

# ====================================================================
# INSTRUCCIONES DE USO:
# ====================================================================
#
# 1. Copiar este archivo a .env:
#    cp .env.example .env
#
# 2. Editar .env y completar los valores reales:
#    nano .env
#
# 3. Para obtener la IP local automáticamente:
#    hostname -I | awk '{print $1}'
#
# 4. Asegurar que el directorio de logs existe:
#    sudo mkdir -p /var/log/asterisk
#    sudo chown asterisk:asterisk /var/log/asterisk
#
# 5. Para probar manualmente:
#    export $(cat .env | xargs) && python3 inbound_calls/handle_incoming_call.py
#
# ====================================================================
